{
  "name": "@bigboat/mqtt-client",
  "version": "1.1.1",
  "description": "Simple MQTT client strictly for BigBoat needs",
  "main": "index.js",
  "keywords": [
    "mqtt",
    "bigboat"
  ],
  "author": "greyarch",
  "license": "ISC",
  "scripts": {
    "test": "nyc --check-coverage --lines 60 --reporter=lcov --reporter=text --reporter=html mocha",
    "test:mutation": "stryker run"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "mqtt": "^2.15.0"
  },
  "devDependencies": {
    "mocha": "^3.5.3",
    "nyc": "^11.2.1",
    "stryker": "^0.10.3",
    "stryker-api": "^0.9.0",
    "stryker-baseline-reporter": "^1.0.2",
    "stryker-html-reporter": "^0.8.0",
    "stryker-mocha-framework": "^0.5.0",
    "stryker-mocha-runner": "^0.8.1",
    "testdouble": "^3.2.6"
  }
}
